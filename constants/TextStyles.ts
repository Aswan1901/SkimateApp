import { StyleSheet } from 'react-native';

export const TextStyles = StyleSheet.create({
    cardTitle: {
        fontSize: 18,
        fontWeight: 'bold',
    },
    bodyText: {
        fontSize: 12,
    },
});